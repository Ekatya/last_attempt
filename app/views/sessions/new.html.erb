<% provide(:tt, "Sign in") %>
<h1>Войти</h1>

 <div class="row">
 <div class="spam6 offset3">
 <%= form_for(:session, url: sessions_path) do |f| %>

	<%= f.label :email %>
	<%= f.label :password %>

	<%= f.password_field :password %>
	<%= f.text_field :email %>
	
	<%= f.submit "Sign in", class: "btn btn-large btn-primary" %>
   <% end %>

   <p> New user? <%= link_to "Sign up now!", signup_path %> </p>
   </div>
</div>
